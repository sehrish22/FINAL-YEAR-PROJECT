extends ../layout

block content
    link(rel='stylesheet' href='/stylesheets/register.css')
    .signup-container
        h1 SIGN UP
        form(action="/users/register", method="post", class="signup-form")
            .signup-form-group
                label(for="name") Name
                input#name(type="text", class="signup-input", name="name", placeholder="Enter your name", required) 
                if errors && errors.name
                    p.error-message= errors.name
            .signup-form-group
                label(for="email") Email
                input#email(type="email", class="signup-input", name="email", placeholder="Enter your email", required) 
                if errors && errors.email
                    p.error-message= errors.email
            .signup-form-group
                label(for="contact") Contact
                input#contact(type="contact", class="signup-input", name="contact", placeholder="Enter your phone number", required)
            .signup-form-group
                label(for="storeName") Store Name
                input#storeName(type="storeName", class="signup-input", name="storeName", placeholder="Enter your Stoe Name")  
            .signup-form-group     
                label(for="password") Password
                input#password(type="password", class="signup-input", name="password", placeholder="Create a password", required)
                p.password-hint Password must be at least 8 characters, with 1 digit and 1 special character.
                if errors && errors.password
                    p.error-message= errors.password
            .signup-form-group
                label(for="role") Account Type
                select#role(name="role", class="signup-input", required)
                    option(value="seller") Seller
                    option(value="buyer") Buyer
                    //- option(value="admin") Admin
                if errors && errors.role
                    p.error-message= errors.role
            .signup-form-group
                label(for="image") Your Image
                input(type="file", class="form-control", id="image", name="image")
                if errors && errors.image
                    p.error-message= errors.image
            .signup-form-group
                input(type="submit", class="signup-btn btn-block", value="SIGN UP")

        .signup-links
            p Already have an account?
            a(href="/users/login", class="signup-link") Log In
